{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
        <h1>Veuillez vous inscrire</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button>{{ button_label|default("S'inscrire") }}</button>
        {{ form_end(form) }}
{% endblock %}