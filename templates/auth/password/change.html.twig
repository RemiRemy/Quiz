{% extends 'base.html.twig' %}

{% block title %}Changer de mot de passe{% endblock %}

{% block body %}
    <h1>Changer de mot de passe</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>{{ button_label|default("Changer mon mot de passe") }}</button>
    {{ form_end(form) }}
{% endblock %}