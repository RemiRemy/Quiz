{% extends 'base.html.twig' %}

{% block title %}Ajouter une question{% endblock %}

{% block body %}
    <h1>Ajouter une question</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>{{ button_label|default("Ajouter") }}</button>
    {{ form_end(form) }}
{% endblock %}